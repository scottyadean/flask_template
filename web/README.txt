### Flask / Mongo / Docker Starter Template
A simple barebone started template for Flask. 

### Setup
pyenv versions
pyenv use 3.9.10
pip install flask
pip install --upgreade pip
export FLASK_APP=app.py; flask run
mkdir -pv ~/mongodb/database



## Run the Project
docker-compose build
docker-compose up